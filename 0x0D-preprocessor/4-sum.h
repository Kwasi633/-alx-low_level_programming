#ifndef SUM_H
#define SUM_H

/*
 * File: 4-sum.h
 * Descp: Header that defines a fun macro SUM(x, y)
 * that computes the sum of the numbers x and y.
 * Headerfile guarded to avoid double inclusion.
 */

#define SUM(x, y) ((x) + (y))

#endif
